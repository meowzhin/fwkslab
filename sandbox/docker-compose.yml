services:
  
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    ports:
      - "9000:80"
    volumes:
      - vaultwarden_data:/data/

volumes:
  vaultwarden_data:
